version: 0.0
os: linux

hooks:
  ApplicationStop:
    - location: script/stop.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: script/start.sh
      timeout: 300
      runas: root
